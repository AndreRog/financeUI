import { PagePlaceholder } from '@/components/PagePlaceholder'

/** Monthly trend across a selected year (issue 0012). */
export function TrendScreen() {
  return (
    <PagePlaceholder
      title="Trend"
      intent="Monthly income, expense and savings across a year — so you can see how you're tracking."
    />
  )
}
