import { PagePlaceholder } from '@/components/PagePlaceholder'

/** Period comparison + drill-down (issue 0013). */
export function ComparisonScreen() {
  return (
    <PagePlaceholder
      title="Comparison"
      intent="Compare two periods side by side (e.g. March 2025 vs March 2024) with per-category deltas, and drill into a category's subcategories."
    />
  )
}
