import { Chart, Line } from 'react-chartjs-2';
import React, { useState } from "react";

interface LineProps {
        title: string;
}

const LineChart: React.FC<LineProps> = ({ name }) => {


        return (
                <div></div>
        );

}
